import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // Real tests will be added per feature as they are built.
    expect(true, isTrue);
  });
}
